# Keyboard shortcuts (future web version)

**Status:** For when we build the web version. Not implemented in the app yet.

Suggested shortcuts to list in-app (e.g. `?` or footer "Shortcuts"):

| Shortcut | Action |
|----------|--------|
| `N` | New audit (focus Hiring / start new audit flow) |
| `/` | Focus Meridian Voice input |
| `Esc` | Close modal / drawer / picker |
| `?` | Show shortcuts help |

Optional later:

- `G` then `C` / `H` / `I` / `V` / `K` — Go to Center / Hiring / Insights / Voice / Calendar
- `J` — Open Jobs page
