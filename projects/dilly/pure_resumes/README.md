# Pure resumes (raw extraction)

Raw text extracted directly from PDFs—no cleaning or sectioning.

- **Naming:** `pure_{email}.txt` (e.g. `pure_sydneyroux06@gmail.com.txt`)
- **Pairing:** Each file pairs with `parsed_resumes/{email}.txt` for format auditing via `audit_formatting()`.
