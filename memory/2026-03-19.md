# 2026-03-19

- Continued certifications work: verified no stale `resourcesSubView` references; updated `docs/WHATS_IN_THE_APP.md` (Get Hired + optional GET `/certifications` API note) and `projects/dilly/IDEAS.md` (Implemented: `/certifications` page + Voice handoff).
- **Dashboard CI / tsc:** Dropped `.next/dev/types/**/*.ts` from `tsconfig.json` include (conflicted with `.next/types` LayoutRoutes). Fixed `VoiceAvatar` invalid `size="lg"` → `md` + Tailwind override in `actions/page.tsx` and `VerdictLoadingScreen.tsx`. `npx tsc --noEmit` now exits 0.
- **GitHub CI:** `.github/workflows/ci.yml` — added `dilly-dashboard-lint` (eslint + `tsc --noEmit`), `dilly-dashboard-test`, `dilly-dashboard-build` for `projects/dilly/dashboard`; renamed Meridian job *names* to “Meridian dashboard …” (job ids `dashboard-*` unchanged for branch protection compatibility).
- **Dilly build / Suspense:** `ats/layout.tsx` wraps `ATSProvider` in `<Suspense>` (fixes `useSearchParams` in ATSContext). `ready-check/new/page.tsx` — single default export wrapping `ReadyCheckNewInner` in `<Suspense>` (removed duplicate export). `next.config.ts` — `turbopack.root` = dashboard dir. `npm run build` passes; keep `.next/dev/types` out of `tsconfig.json` include (Next may re-add after build — strip for CI `tsc`).
