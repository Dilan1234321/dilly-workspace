# 2025-03-07

- **High priority (P0): Master UI/UX.** One consistent UI for onboarding and main app: add shadcn/ui to dashboard, theme with DESIGN_SYSTEM.md, use shared Button/Input/Card everywhere. See `projects/meridian/docs/UI_UX_PRIORITY.md` and ROADMAP.md “High priority” section. Do in parallel with or before Phase 2.
- **High priority, later:** Resend (verification emails); Stripe (live checkout). See ROADMAP Phase B.
- **Roadmap created:** `docs/MERIDIAN_ROADMAP.md` — detailed step-by-step roadmap for Meridian (Phase 1 completion, auth, app flow, parsing improvements, maintenance). Dilan away for a couple of hours; roadmap is for when he returns so every step is explicit.
- **Earlier in session:** Parsing improvements (reflow, contact promotion, email filenames, `user_email` optional param); deleted `Pennsbury_Highschool.txt`; filename = email when available (parser-extracted for now; auth will send session email later).
- **Phase 1 status:** Evidence trail UI done (click dimension → show evidence). Still to do: Anomaly (red_flags), Peer benchmarking (percentiles from training_data.json). Auth not built yet (spec only).
- **Auth + paywall + red flags + peer benchmarking:** Magic-link auth (Option A) with persistent session (30-day token in localStorage). Paywall $20/month (no first-audit-free; copy: "Subscribe to run audits and full reports"). Red flags module in dilly_core/red_flags.py; dashboard shows muted red UI. Peer benchmarking: percentiles vs same-track cohort (training_data.json + meridian_audit_log.jsonl). To enable the first account: first user is auto-subscribed. For an existing account, set `MERIDIAN_DEV=1` when running the API, then click "Dev: unlock my account" on the paywall.
