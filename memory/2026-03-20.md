- **Dilly dashboard:** Built premium **8-screen onboarding** at `/onboarding/*` (welcome → results): `lib/onboarding/*`, `components/onboarding/*`, `sessionStorage` state, `OnboardingProvider` in `Providers.tsx`. Docs: `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly dashboard:** **`/` replaces legacy inline onboarding** — `page.tsx` gates with `allowMainApp` + `getResumeOnboardingPath()`; unauthenticated → `/onboarding/welcome`; incomplete → resume route; `ensureSchoolStorageFromEmail`; removed magic-link / school-theme / steps 4–6 UI; `profileFetchDone` + audit history loading wait before mis-redirect; cached profile normalizes `onboarding_complete`. `mainAppEntry.ts`, `WHATS_IN_THE_APP.md`.
- **Dilly onboarding profile:** Multiple **majors** and **minors** (session state `majors[]` / `minors[]`, legacy `major`/`minor` migrated from sessionStorage). `PATCH /profile` sends `name`, `major` (primary), `majors`, `minors`, `track`, `preProfessional`, `application_target`, `application_target_label`. Results step **re-PATCHes** profile with those fields plus `onboarding_complete`. `state.ts`, `profile/page.tsx`, `results/page.tsx`.
- **Dilly onboarding returning users:** Welcome redirects to **`/`** if `AUTH_TOKEN` + `GET /auth/me` succeeds. After verify-code, **`isReturningUserProfile`** (same criteria as home gate) → **`/`**; else profile step. Auth user cache + `ensureSchoolStorageFromEmail`. `mainAppEntry.ts`, `welcome/page.tsx`, `verify/page.tsx`.
- **Dilly Recruiter:** Sort includes **Top %** with a second dropdown (Smart·Grit·Build avg, Final, ATS, or General blend). API: `peer_benchmark.get_peer_percentile_final`, candidate rows carry `top_pct_*`; `POST /recruiter/search` sort keys `top_pct_sgb` | `top_pct_final` | `top_pct_ats` | `top_pct_general`. Removed client-side re-sort that always forced match_score. `recruiter/page.tsx`, `recruiter_search.py`, `peer_benchmark.py`.
- **Dilly dashboard:** Global pull-to-refresh: `GlobalPullToRefresh` in `Providers` reloads the page when user pulls down at scroll top (touch). Removed duplicate listeners from `page.tsx` (Career Center) and `jobs/page.tsx`. `docs/WHATS_IN_THE_APP.md`.
- **Resume edit save:** Auto-save no longer sets global `saving` (no “Saving…” / disabled Save every 2s). `doSave` uses `sourceAuditIdRef` + `doSaveRef` for correct audit id and no stale auto-save closure. Save uses `fetchWithTimeout` 45s. API: `POST /resume/save` writes compact JSON and `await asyncio.to_thread(_save_resume)` so disk IO doesn’t block the event loop.
- **Dilly ATS scan hang:** `fetchAuthed` had no timeout (fetch could hang indefinitely). All ATS scan requests now use `fetchWithTimeout` (e.g. 95s analysis, 75s enrich). AbortError → user-facing timeout message. API: `POST /ats-analysis-from-audit` runs parse+analysis in `asyncio.to_thread` with `wait_for` (default 90s, `DILLY_ATS_ANALYSIS_TIMEOUT_SEC`) and returns 504 on overrun.
- **Dilly ATS scan perf:** `ATSContext` runs history + analysis in parallel, then record + vendor + keyword + rewrite in parallel; vendor POST includes `ats_analysis` so API skips duplicate `run_ats_analysis`; scan rewrite uses `use_llm: false` (rule-based) to avoid LLM latency. API: `ats_analysis_result_from_dict`, `/ats-vendor-sim` accepts `ats_analysis`, `/ats-rewrite` accepts `use_llm`.
- **Dilly ATS layout:** Fixed huge empty band between ATS content and fixed bottom docks: `ats/layout.tsx` now uses a flex column + `flex-1` main column, renders `CareerCenterMinibar`/`BottomNav` outside that column, and adds `ats-root` with `body:has(.ats-root)` in `career-center-talent.css` (same cool `--bg` as Career Hub, no warm grain in the gap).
- **Dilly:** Pre-professional profile selection now drives cohort label: `getEffectiveCohortLabel` in `trackDefinitions.ts` maps UT pre-prof paths to **Pre-Health** or **Pre-Law** for header, edit profile, Vs Your Peers, playbooks, share card, Voice context, `/profile/details` (cohort + optional path line). Docs: `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly dashboard:** Fixed “Loading your previous audit…” stuck after sign-in: home now paints scores from GET `/audit/history` immediately (`minimalAuditFromHistorySummary`), then upgrades from GET `/audit/history/{id}` with timeouts (15s / 20s). Stale localStorage is ignored when server latest id differs. Same pattern + timeouts in `ATSContext.tsx`.
- **Dilly share card (Send to friends):** Desktop Chromium often can’t use `navigator.share({ files })`; the old code then called `share({ text, url })` so recipients only saw text + link. Now we use `navigator.canShare({ files })` and open the **Share card** sheet (same as WebKit) when file share isn’t available or fails; the sheet copies/downloads the PNG then opens a text share so the user can paste/attach the image. `page.tsx` + `docs/WHATS_IN_THE_APP.md`.
- **Dilly interview prep (evidence):** Moved from Get Hired to Career Center; **Interview Prep** tool button calls `handleInterviewPrepFromEvidence` (`POST /interview-prep`). Removed voice `/voice/interview-prep` flow from Center. Results use nested `<details>` (dimension → Question / Strategy / 30-second script) for phone. `useToast` hoisted to top of `Dashboard` for handler access. `page.tsx`, `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly Career Center:** Gap Analysis, Cover Letter, and Interview Prep no longer call `setMainAppTab("resources")`; results render in cards under the tool row on Center. Get Hired prep tools: no gap scan or cover letter (both Career Center only). `docs/WHATS_IN_THE_APP.md`.
- **Dilly Career Center:** Interview Prep and Achievements are in one `grid-cols-2` row (removed Interview Prep `col-span-2` and the separate full-width Achievements block). `docs/WHATS_IN_THE_APP.md` updated.
- **Dilly:** Removed all “first audit” pressure UX: no modal/banner/auto-switch to Hiring; neutral empty states; Voice first-visit greeting generic; `getProactiveNudge` null without audit; certifications empty → Back; jobs/resume-edit/companies/ATS copy softened; `IDEAS.md` + `docs/WHATS_IN_THE_APP.md` updated.
- **Dilly Get Hired:** Job search checklist → **Job search playbook**: 3 phases (story / pipeline / convert), 12 items with title + actionable hint, progress bar + %, per-phase counts, Ask Dilly + reset (writes `{}` to `meridian_job_checklist_*`). `page.tsx`, `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly Get Hired:** Job checklist rows: **`VoiceAvatar` xs** at end of title → Voice `get_hired_job_checklist` + step prompt; `openVoiceFromScreen` optional convo title; `voice_helpers.py` + `voice_app_features.json`.
- **Dilly:** `/career-playbook` personalized playbook (POST `/audit/career-playbook` + alias `/career-playbook`, LLM + fallback); client tries audit path first. Get Hired **View full playbook** → that route; friendly 404 copy. `DILLY_PLAYBOOK_VOICE_PROMPT_KEY` overlay; `view=playbook` deep link.
- **Dilly Voice:** Removed proactive “quick insight” first assistant message on empty chat (deadline / score trajectory / top rec / `getProactiveNudge`). `page.tsx`; `IDEAS.md`, `WHATS_NEXT.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly Voice inline visuals:** Segment pipeline in `VoiceAssistantRichReply` (`voiceMessageVisuals.ts` / `voiceVisualTypes.ts`): scores, top recs, deadline timeline, interview agenda, before/after, chips, steps; staged CSS build animations (`globals.css`); `DillyVoiceChatScoresBundle` on `page.tsx` + `VoiceOverlay`; `_VOICE_INLINE_VISUALS_BLOCK` in `voice_helpers.py`. `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly bottom nav:** Tapping **Dilly AI** calls `openVoiceResumeRecentChat` — opens overlay on **most recent** saved convo by `updatedAt`, not `openVoiceWithNewChat()`. `page.tsx`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly Voice duplicate replies / “on .”:** Stream handler now commits the main assistant bubble **once** (`assistantReplyCommitted`), only appends token chunks when `evt.t` is a non-empty string, uses `evt.done === true` (not `else if` after `t`), post-stream fallback only if nothing committed; `sanitizeVoiceAssistantReply` on stream + `/voice/chat` fallback; duplicate `setVoiceMemory` summary removed from done tail. API prompt: never emit broken date phrases. `page.tsx`, `voiceReplySanitize.ts`, `voice_helpers.py`.
- **Dilly Voice calendar:** Extracted deadlines are **auto-saved** to profile on the API (`_persist_voice_detected_deadlines` in `voice.py`); responses use `deadlines_auto_saved` (not `detected_deadlines`). Client merges + toast; confirmation card removed. Prompt: do not ask permission to add to calendar; say it was added.
- **Voice deadline extraction fix:** `client_local_date` (browser `en-CA` local YYYY-MM-DD) in `buildVoiceContext()`; `_heuristic_deadlines_from_user_message` + ordinal resolution (`on the 24th` with `with Company`) and typo-tolerant interview detection; LLM extract uses same “today” and ordinal rules. Fixes empty calendar after “Raymond James on the 24th” style messages.
- **Dilly Voice:** Composer **auto-focus** on open — `DillyVoicePrompt` `autoFocus` (double `requestAnimationFrame` + `preventScroll`); `VoiceInputWithMic` + `VoiceOverlay` pass it through. Main tab: only when `mainAppTab === "voice"` and not overlay / chat list / remember / bullet rewriter (avoids stealing focus from hidden off-screen composer). Overlay: `autoFocus={!chatListOpen}` so closing the drawer returns focus to the pill.
- **Dilly Voice empty greeting:** One-time long “I’m Dilly, your career coach…” intro per account (`dilly_voice_intro_seen_v1_{email}`); returning = `Hey {name}, what's on your mind?` / no name fallback. Migration: any saved convo with messages marks seen; also mark on first send or on leaving Voice/overlay. Replaces old contextual greetings (audit/deadlines/etc.). `dillyUtils.ts` + `VoiceOverlay` `emptyChatGreeting`.
- **Dilly Voice grounding (“what’s coming up”):** `buildVoiceContext` sends `pipeline_context` (GET `/habits` + proactive nudges): upcoming deadlines (habits, up to 12), `pipeline_counts` applied/interviewing/offers, apps week/month, silent follow-ups, funnel, urgent deadline. `/habits` adds `pipeline_counts`, extends `upcoming_deadlines` to 12. `voice_helpers.format_voice_user_content` sorts profile calendar futures (up to 15) with days-until; system prompt **Answer from their profile** block forbids generic deflection when context has facts.
- **Dilly output safety (slurs):** Server `output_safety.py` — `sanitize_user_visible_assistant_text` / `sanitize_voice_reply_and_suggestions` (redirect **Let's try to keep this professional.**); voice `/voice/chat`, `/voice/stream` (buffer stream → one safe payload, no token leakage), mock interview JSON walk, rewrite-bullet, interview-prep strings; recruiter Ask AI streams buffered + sanitized; `_ASK_AI_SYSTEM_PROMPT` + stricter `voice_helpers` inappropriate block. Client mirror: `voiceReplySanitize.ts` + `VOICE_ASSISTANT_SLUR_REDIRECT`.
- **Dilly Voice “Your  score” blank:** `healEmptyVoiceDimensionTags` now tolerates whitespace inside `[ grit ]` pairs, strips ZWSP/NBSP, and when the prior user message names Smart/Grit/Build rewrites `**Your score** is|was|…` into tagged `Dim score of N` (e.g. after “what’s my grit score?”). `VoiceAssistantRichReply` takes `priorUserContent`; `page.tsx` + `VoiceOverlay.tsx` pass the previous user bubble. `voiceDimensionMarkup.test.ts`.
- **Dilly Career Center background seam (new user):** Subscribed shell `.app-talent` and `.career-center-talent` default bg were `var(--te-bg-deep)` (#040a16) while panels used `var(--bg)` (#0A0A0C), so the lower viewport / frosted dock showed a navy band. Unified shell + CC class to `var(--bg)`; `body:has(.app-talent)` gets same flat bg (no warm grain) for overscroll. `app-talent.css`, `career-center-talent.css`.
- **Dilly Voice — more inline visuals + recap + tone + celebrations:** Markers/blocks `application_card`, `next_moves`, `story_timeline`, `peer_context_visual`; dedup extended; `scoresAuthoritative` for no-audit bundle; GET `/applications` with habits; `format_voice_user_content` applications + peer + `_infer_tone_mirror_mode`. Career Center `VoiceSessionRecapCard` + `voiceSessionRecap.ts`. `dillyMicroCelebrations.ts` (streak 7, first audit, first app). `voice_helpers.py`, `IDEAS.md`, `WHATS_IN_THE_APP.md`, `voice_app_features.json`.
- **Dilly Voice — in-chat mock interview:** Practice **Mock interview** → `openVoiceWithNewChat("Start a mock interview.", "Mock interview")`; `wantsMockInterview` / `wantsEndMockInterview` in `voiceMockInterview.ts`; while session active, `sendVoice` calls `POST /voice/mock-interview` instead of stream; `VoiceMockInterviewUI` cards + banner; `VoiceConvo.messages` optional `mockTurn`; overlay `mockInterviewBanner`. `/mock-interview` page unchanged for optional setup.
- **Profile details loading:** Tapping profile photo → `/profile/details` no longer uses full-page `LoadingScreen`. `useLayoutEffect` hydrates from `dilly_profile_cache_*` + photo without requiring fresh auth cache TTL; email from session/local auth cache or JWT; cold load uses in-page skeleton (header + pulses) instead of blocking loader.
- **Dilly My Score (`/score`):** New route + `GET /profile/score-page` (`score_page.py`), components under `components/score/`, Voice gap handoff `DILLY_SCORE_GAP_VOICE_PROMPT_KEY`, Hiring Score hub → redirect, ScoreCard number → `/score`, minibar link `/score`.
- **Dilly Leaderboard (`/leaderboard`):** `app/leaderboard/page.tsx` + `components/leaderboard/*`, `GET /leaderboard/page/{track}`, session cache + post-audit `DILLY_LEADERBOARD_REFRESH_KEY`, Voice `DILLY_LEADERBOARD_VOICE_PROMPT_KEY` + `context=leaderboard`, bottom nav **Rank** (main + score pill + sibling shells), Insights “Weekly leaderboard” link, peer preview track query. `IDEAS.md`, `WHATS_IN_THE_APP.md`.
- **Dilly Jobs under Get Hired:** **`JobsPanel`** on **Get Hired** with sub-tabs **Applications | Jobs**; **`/jobs`** redirects to **`/?tab=resources&view=jobs`**. Removed **Jobs** from bottom nav (home + ATS, score, leaderboard, certifications, playbook, audit shells). In-app links use **`/?tab=resources&view=jobs`** (+ **`&type=`**). `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly application tracker:** Moved **ApplicationsSection** to **Get Hired** on home (`page.tsx` — Pipeline / Application tracker; deep link `view=applications` + scroll). **`/applications`** and **`/career?tab=applications`** → Get Hired tracker. Docs: `IDEAS.md`, `WHATS_NEXT.md`, `docs/WHATS_IN_THE_APP.md`.
- **Dilly nav + `/career`:** Bottom nav is **Career Center | Rank | Dilly AI | Get Hired** (four tabs; no **Practice** pill — Practice via minibar / `/?tab=practice` / `/career` redirect). **`/career`** → **`/?tab=practice`** (`?tab=applications` → Get Hired). **CareerCenterMinibar** Practice link + highlight on Practice panel. `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
- **Recruiter:** `setTotal(deduped.length…)` fix (was broken `sorted`).
- **Dilly launch sequence:** `AppLaunchSequence` + `LoadingScreen` / `SplashScreen` / `useSplashState`; `DillyFaceEngine` + `DillyAvatar`; `GET /profile/splash-state` (`splash_state.py`); `launch-tokens.css`, `lib/launch/splashConfig.ts`; Playfair in `layout.tsx`; voice deep links for splash contexts. `IDEAS.md`, `docs/WHATS_IN_THE_APP.md`.
