# 2026-03-16

- **Cousin feedback on Meridian:** (1) Master the backend, make it kickass. (2) Streamline the UI — lots of features, users scroll a long way. Captured and turned into an action plan in `docs/COUSIN_FEEDBACK_BACKEND_AND_UI.md` (routers + errors + logging for API; hero block + collapsible/More + quick links for Career Center).
- **Started cousin-feedback follow-up:** Updated `docs/COUSIN_FEEDBACK_BACKEND_AND_UI.md` — Voice router no longer stubbed (reimplemented with data-capture). API structure section now notes request logging + central exception handler. Backend: added request_id (X-Request-ID header or generated), included in log line and in all error responses (envelope + header); exception handler always returns request_id in JSON and X-Request-ID on error responses.
- **Career Center UI streamlining (cousin feedback):** Hero block (scores or “Run first audit” + one do-next + View Report / Ask Meridian). Compact tool row: ATS, Jobs, Recruiter profile. Collapsible “More from your career center” (default collapsed). Sticky Quick links bar when on Center tab: Report, New audit, Jobs, Calendar, Insights, Stickers — fixed above bottom nav so key actions are one tap without long scroll. State: centerMoreOpen in page.tsx.
